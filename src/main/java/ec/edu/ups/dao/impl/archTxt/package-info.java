/**
 * <p>Este paquete contiene las implementaciones de los objetos de acceso a datos (DAO)
 * que gestionan la persistencia de la información en archivos de texto plano (CSV u otros formatos estructurados).</p>
 *
 * <p>Las clases en este paquete son responsables de leer y escribir datos de objetos
 * en formato de texto. Esto implica un proceso de conversión explícito de los atributos
 * de los objetos a cadenas de texto y viceversa. Son útiles para una inspección humana
 * directa del archivo de datos.</p>
 */
package ec.edu.ups.dao.impl.archTxt;