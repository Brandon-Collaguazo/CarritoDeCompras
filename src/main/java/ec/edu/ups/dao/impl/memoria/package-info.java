/**
 * <p>Este paquete contiene las implementaciones en memoria de los objetos de acceso a datos (DAO).</p>
 *
 * <p>Las clases en este paquete gestionan los datos directamente en la memoria RAM de la aplicación,
 * lo que proporciona un acceso muy rápido a la información. Sin embargo, los datos persisten
 * solo mientras la aplicación se está ejecutando; se pierden una vez que el programa finaliza.</p>
 *
 * <p>Estas implementaciones son ideales para pruebas unitarias, desarrollo inicial o para
 * escenarios donde la persistencia a largo plazo no es un requisito (ej., datos temporales).</p>
 */
package ec.edu.ups.dao.impl.memoria;